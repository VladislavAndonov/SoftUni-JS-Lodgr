import { Owner } from "./owner";

export interface Space {
    // id: string;
    name: string;
    image: string;
    location: string;
    price: number;
    // description: string;
    // owner: Owner;
}